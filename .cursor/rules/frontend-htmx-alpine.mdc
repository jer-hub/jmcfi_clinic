---
description: Frontend standards — HTMX for server communication, Alpine.js for local state, zero vanilla JavaScript
globs: "**/*.html,**/*.js"
alwaysApply: false
---

# Frontend: HTMX + Alpine.js

## Core Rule

**HTMX for server communication. Alpine.js for local state. Zero vanilla JavaScript.**

Canvas dental charting lives inline in `dental_records/templates/.../interactive_dental_chart.html` (acceptable exception).

## HTMX Patterns

### Server Communication

```html
<!-- Fetch content -->
<button hx-get="/api/data" hx-target="#result" hx-swap="innerHTML">Load</button>

<!-- Form submission -->
<form hx-post="/submit" hx-target="#response">
  {% csrf_token %}
  <input name="field" required>
  <button type="submit">Submit</button>
</form>

<!-- Search with debounce -->
<input type="text" name="q"
  hx-get="/search" hx-target="#results"
  hx-trigger="input changed delay:300ms">

<!-- Delete with confirmation swap -->
<button hx-delete="/items/{{ item.pk }}"
  hx-target="closest .item-row" hx-swap="outerHTML swap:300ms"
  hx-confirm="Are you sure?">Delete</button>
```

### Pagination

```html
<button hx-get="?page={{ page_obj.next_page_number }}"
  hx-target="#item-list" hx-swap="innerHTML"
  hx-push-url="true">Next</button>
```

## Alpine.js Patterns

### Toggles & Modals

```html
<div x-data="{ open: false }">
  <button @click="open = !open">Toggle</button>
  <div x-show="open" x-transition x-cloak>Content</div>
</div>
```

Prefer the shared clinic modal (`templates/components/modal.html` + `__jmcfiDispatchOpenModal`) over bespoke overlays when possible.

### Form Validation (client-side)

```html
<form x-data="{ email: '' }" @submit.prevent="$el.submit()">
  <input x-model="email" type="email" required>
  <span x-show="email && !email.includes('@')" class="text-red-500 text-sm">Invalid email</span>
</form>
```

## HTMX + Alpine Integration

```html
<div x-data="{ loading: false }">
  <button hx-post="/action" hx-target="#result"
    @htmx:before-request="loading = true"
    @htmx:after-settle="loading = false">
    <span x-show="!loading">Submit</span>
    <span x-show="loading"><i class="fas fa-spinner fa-spin"></i></span>
  </button>
</div>
```

## Forbidden Patterns

| Never use | Replace with |
|-----------|-------------|
| `addEventListener()` | `@click`, `@change`, `hx-trigger` |
| `document.querySelector()` / `getElementById()` | `x-ref`, `hx-target` |
| `innerHTML` / `textContent` assignment | `x-text`, `x-html`, `hx-swap` |
| `fetch()` / `XMLHttpRequest` | `hx-get`, `hx-post`, `hx-put`, `hx-delete` |
| `setTimeout` / `setInterval` | `hx-trigger="every 2s"`, Alpine `$watch` |
| `onclick="..."` attribute | `@click="..."` (Alpine) |
| Global `<script>` functions | `x-data` components or server endpoints |
| Floating toasts / `showToast` | Django `messages` inline banners |

## Decision Guide

- Server communication needed? → **HTMX**
- Local UI state (toggle, dropdown, tab)? → **Alpine.js**
- Both? → Alpine for state, HTMX for server calls
- Canvas / WebGL / complex graphics? → Vanilla JS is acceptable (isolate in templates or `staticfiles/js/`)

## Refactoring vanilla JS (when you find it)

Priority: forms → toggles → modals/dropdowns → search/filter → inline edit. Keep canvas/SVG as-is.

Checklist:

1. Map vanilla JS for the feature
2. Design HTMX + Alpine replacement
3. Handle `is_htmx_request()` in the view; return `status=400` on validation errors
4. Add a partial for HTMX swaps; update the full page
5. Test HTMX and full navigation; remove old `<script>` blocks

| Vanilla JS | HTMX/Alpine |
|-----------|-------------|
| `fetch('/url', {method: 'POST', …})` | `<form hx-post="/url">` |
| `getElementById('x').innerHTML = y` | `hx-target="#x" hx-swap="innerHTML"` |
| `el.style.display = …` | `x-show="condition"` |
| `addEventListener('click', …)` | `@click` or `hx-trigger="click"` |
| `classList.add/remove/toggle` | `:class="{ 'active': condition }"` |
| debounce `setTimeout` | `hx-trigger="input changed delay:300ms"` |
| `JSON.parse` then DOM update | Return HTML fragment from Django |
