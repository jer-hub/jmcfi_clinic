# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
venv

# Local environment secrets (never commit — copy from .env.example)
.env
.env.*
!.env.example

# Supabase CLI local state
supabase/.branches
supabase/.temp

# Media files (uploaded content)
media/

# collectstatic output (WhiteNoise manifest + gzip). Source assets live in staticfiles/.
# Dockerfile runs collectstatic on deploy; do not commit generated files under static/.
static/staticfiles.json
static/**/*.gz
# Manifest-hashed filenames: name.<12-char-hash>.ext (or extensionless name.<hash>)
static/**/*.*????????????.*
static/**/*.*????????????
# Copies of staticfiles/ assets emitted into STATIC_ROOT
static/js/
static/css/
static/fonts/
static/logo/
static/favicon/
static/img/
# Vendor/package static copied into STATIC_ROOT by collectstatic
static/admin/
static/django_extensions/
static/account/

# One-off audit script output (do not commit)
audit_results.json

.agents
.github
.vscode
.instructions.md
.clinerules

# Node / Tailwind build
node_modules/
.DS_Store

.cursorrules
.cursor/skills/

# Local SQLite dev database (never commit)
db.sqlite3